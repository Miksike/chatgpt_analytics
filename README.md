This is a minimal local Python project for exploring exported ChatGPT data in VS Code on Windows.

Place your export inside `raw_export`, use scripts in `scripts`, and keep generated outputs in `working_data` and `reports`.